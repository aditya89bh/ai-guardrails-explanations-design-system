/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standalone static site — no server-side data fetching required
  // All decision engine logic runs client-side
  reactStrictMode: true,
};

module.exports = nextConfig;
